# ============================================================================
# 🏇 Docker Compose - Stack Web (Backend + Frontend)
# ============================================================================
# Services:
# - backend: API FastAPI (port 8000)
# - frontend: React + Nginx (port 80)
#
# NOTE: Utilise la base de données PostgreSQL existante (pmuBDD sur port 54624)
# ============================================================================

version: '3.8'

services:
  # ==========================================================================
  # BACKEND FASTAPI
  # ==========================================================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: horse-backend
    ports:
      - "8000:8000"
    environment:
      # Connexion à la BDD existante (pmuBDD) via host.docker.internal
      - DATABASE_URL=postgresql://pmu_user:pmu_secure_password_2025@host.docker.internal:54624/pmu_database
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GEMINI_API_KEY=AIzaSyBi_o5kQJ9Nl1OR-_y2axv2Iv5nLKI9f7w
      - AI_PROVIDER=${AI_PROVIDER:-auto}
      - LOG_LEVEL=info
      # Activer le modèle Champion (ré-entraîné avec features runtime)
      - USE_CHAMPION_MODEL=1
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      # Pour le développement : hot-reload
      - ./backend:/app
      # Accès aux modèles et configurations du projet principal
      - ../models:/project/models:ro
      # Modèle champion XGBoost + preprocessing
      - ../data/models/champion:/project/data/models/champion:ro
      - ../config:/project/config:ro
      - ../race_pronostic_generator.py:/project/race_pronostic_generator.py:ro
      - ../calibration:/project/calibration:ro
      - ../artifacts:/project/artifacts:ro
      # Module d'analyse IA
      - ../ai_results_analyzer.py:/project/ai_results_analyzer.py:ro
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-sf", "http://localhost:8000/healthz" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - horse-network

  # ==========================================================================
  # FRONTEND REACT + NGINX
  # ==========================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: horse-frontend
    ports:
      - "80:80"
    environment:
      # Configuration runtime injectée via generate-config.sh
      - VITE_API_BASE_URL=/api
      - APP_ENVIRONMENT=production
      - APP_VERSION=${APP_VERSION:-1.0.0}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-sf", "http://localhost:80/healthz" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - horse-network

# ==========================================================================
# RÉSEAU
# ==========================================================================
networks:
  horse-network:
    driver: bridge
