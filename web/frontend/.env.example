# ============================================================================
# Configuration du Frontend HRP
# ============================================================================

# URL de l'API backend (Vite build-time)
# En production Docker, utilisez plutôt les variables d'env du conteneur
VITE_API_URL=http://localhost:8000/api

# ============================================================================
# Variables Docker (passées au conteneur, pas à Vite)
# ============================================================================
# VITE_API_BASE_URL=/api
# APP_ENVIRONMENT=production
# APP_VERSION=1.0.0
